package com.claudecode.core.message;

import org.apache.commons.lang3.StringUtils;
import java.time.Instant;
import java.util.ArrayList;
import java.util.List;
import java.util.UUID;

/**
 * Factory methods for constructing message objects.
 *
 * <ul>
 *   <li> —
 *       {@code createUserMessage}, {@code createUserInterruptionMessage},
 *       {@code createSyntheticUserCaveatMessage},
 *       {@code createAssistantMessage}, {@code createAssistantAPIErrorMessage},
 *       {@code createCommandInputMessage}, {@code createTurnDurationMessage},
 *       {@code prepareUserContent}, {@code formatCommandInputTags}, and
 *       {@code createModelSwitchBreadcrumbs}
 *   </li>
 *   <li>the assistant row a refusal
 *       leaves behind, whose text comes from {@link RefusalErrorMessage}</li>
 * </ul>
 */
public final class MessageFactory {

    private MessageFactory() {}


    public static UserMessage createUserMessage(String content, boolean isMeta) {
        String text = (StringUtils.isEmpty(content))
                ? MessageConstants.NO_CONTENT_MESSAGE : content;
        return new UserMessage(
                UUID.randomUUID().toString(),
                MessageContent.ofText(text),
                isMeta,
                false,
                null,
                MessageOrigin.USER,
                null,
                Instant.now(),
                null,
                null
        );
    }

    /**
     * Convenience overload: creates a non-meta UserMessage with text content.
     */
    public static UserMessage createUserMessage(String content) {
        return createUserMessage(content, false);
    }


    public static UserMessage createUserMessage(String content, boolean isMeta, boolean isVirtual) {
        String text = (StringUtils.isEmpty(content))
                ? MessageConstants.NO_CONTENT_MESSAGE : content;
        return new UserMessage(
                UUID.randomUUID().toString(),
                MessageContent.ofText(text),
                isMeta, false, null, MessageOrigin.USER, null, Instant.now(),
                null, null, null, null, null,
                isVirtual, null, null);
    }

    /**
     * Creates a UserMessage with content blocks.
     */
    public static UserMessage createUserMessage(List<ContentBlock> blocks, boolean isMeta) {
        return new UserMessage(
                UUID.randomUUID().toString(),
                MessageContent.ofBlocks(blocks),
                isMeta,
                false,
                null,
                MessageOrigin.USER,
                null,
                Instant.now(),
                null,
                null
        );
    }


    public static UserMessage createUserInterruptionMessage(boolean toolUse) {
        String content = toolUse
                ? MessageConstants.INTERRUPT_MESSAGE_FOR_TOOL_USE
                : MessageConstants.INTERRUPT_MESSAGE;

        // interruption rows. They are user-role protocol sentinels, not new
        // human input, so persisting origin=human changes resume semantics.
        return new UserMessage(
                UUID.randomUUID().toString(),
                MessageContent.ofBlocks(List.of(new TextBlock(content))),
                false, false, null, null, null, Instant.now(), null, null);
    }


    public static UserMessage createSyntheticUserCaveatMessage() {
        String tag = "local-command-caveat";
        String content = "<" + tag + ">Caveat: The messages below were generated by the user while running local commands. " +
                "DO NOT respond to these messages or otherwise consider them in your response unless the user explicitly asks you to.</" + tag + ">";
        return createUserMessage(content, true);
    }


    public static AssistantMessage createAssistantMessage(String content) {
        String text = (StringUtils.isEmpty(content))
                ? MessageConstants.NO_CONTENT_MESSAGE : content;
        return new AssistantMessage(
                UUID.randomUUID().toString(),
                AssistantContent.of(List.of(new TextBlock(text)))
        );
    }

    /**
     * Creates an AssistantMessage with content blocks.
     */
    public static AssistantMessage createAssistantMessage(List<ContentBlock> blocks) {
        return new AssistantMessage(
                UUID.randomUUID().toString(),
                AssistantContent.of(blocks)
        );
    }


    public static AssistantMessage createAssistantAPIErrorMessage(String content) {
        return createAssistantAPIErrorMessage(content, null, null);
    }


    public static AssistantMessage createAssistantAPIErrorMessage(
            String content, String apiError, String error) {
        String text = (StringUtils.isEmpty(content))
                ? MessageConstants.NO_CONTENT_MESSAGE : content;
        Usage syntheticUsage = new Usage(
                0, 0, 0, 0,
                Usage.ServerToolUse.ZERO,
                null,
                Usage.CacheCreation.ZERO,
                null,
                null,
                null);
        return new AssistantMessage(
                UUID.randomUUID().toString(),
                AssistantContent.apiResponse(
                    UUID.randomUUID().toString(),
                    List.of(new TextBlock(text)),
                    syntheticUsage,
                    "<synthetic>",
                    "stop_sequence",
                    ""),
                true,
                null,
                Instant.now(),
                null,
                null,
                null,
                null,
                apiError,
                error
        );
    }

    /**
     * The API-error row a refused turn leaves behind. It differs from
     * {@link #createAssistantAPIErrorMessage(String, String, String)} in the three
     * fields a refusal has and an ordinary error does not: the {@code refusal}
     * stop reason, the category and explanation that came with it, and the request
     * id a user can quote when reporting it.
     */
    public static AssistantMessage createRefusalErrorMessage(
            String content, String requestId, StopDetails stopDetails) {
        AssistantMessage base = createAssistantAPIErrorMessage(content, null, "invalid_request");
        return new AssistantMessage(
                base.uuid(),
                base.message().withFinalDelta(null, "refusal", null, stopDetails),
                base.isApiErrorMessage(),
                base.parentUuidValue(),
                base.timestampValue(),
                base.attributionSkill(),
                base.attributionPlugin(),
                base.attributionMcpServer(),
                base.attributionMcpTool(),
                base.apiError(),
                base.error(),
                base.isVirtual(),
                requestId,
                base.advisorModel(),
                base.isMeta()
        );
    }


    public static MessageContent prepareUserContent(String inputString, List<ContentBlock> precedingInputBlocks) {
        if (precedingInputBlocks == null || precedingInputBlocks.isEmpty()) {
            return MessageContent.ofText(inputString);
        }
        List<ContentBlock> blocks = new ArrayList<>(precedingInputBlocks);
        blocks.add(new TextBlock(inputString));
        return MessageContent.ofBlocks(blocks);
    }


    public static SystemMessage createCommandInputMessage(String content) {
        return new SystemMessage(UUID.randomUUID().toString(), "local_command", "info", content);
    }


    public static SystemMessage createSystemMessage(String content, String level) {
        return new SystemMessage(UUID.randomUUID().toString(), "informational", level, content);
    }


    public static SystemMessage createCompactBoundaryMessage(
            String trigger, long preTokens, String lastPreCompactMessageUuid,
            String userContext, Integer messagesSummarized) {
        String content = "Conversation compacted";
        return new SystemMessage(
                UUID.randomUUID().toString(),
                "compact_boundary",
                "info",
                content,
                lastPreCompactMessageUuid,
                Instant.now(),
                new CompactMetadata(
                    trigger, preTokens, null, null, null, null, null,
                    userContext, messagesSummarized, null, null)
        );
    }

    /** Convenience overload with minimal required parameters. */
    public static SystemMessage createCompactBoundaryMessage(String trigger, long preTokens) {
        return createCompactBoundaryMessage(trigger, preTokens, null, null, null);
    }


    public static SystemMessage createMicrocompactBoundaryMessage(
            String trigger, long preTokens, long tokensSaved,
            List<String> compactedToolIds, List<String> clearedAttachmentUUIDs) {
        return new SystemMessage(UUID.randomUUID().toString(), "microcompact_boundary", "info",
                "Context microcompacted");
    }


    public static SystemMessage createSystemAPIErrorMessage(
            String errorMessage, long retryInMs, int retryAttempt, int maxRetries) {
        String content = String.format("API error: %s (retry %d/%d, wait %dms)",
                errorMessage, retryAttempt, maxRetries, retryInMs);
        return new SystemMessage(UUID.randomUUID().toString(), "api_error", "error", content);
    }


    public static ProgressMessage createProgressMessage(
            String toolUseID, String parentToolUseID, ProgressMessage.ProgressData data) {
        String content = (data != null && data.output() != null) ? data.output() : "";
        return new ProgressMessage(UUID.randomUUID().toString(), content, null, Instant.now(),
            toolUseID, parentToolUseID, data);
    }

    /**
     * Legacy string-only overload retained for backward compatibility.
     * @deprecated prefer {@link #createProgressMessage(String, String, ProgressMessage.ProgressData)}.
     */
    @Deprecated
    public static ProgressMessage createProgressMessage(
            String toolUseID, String parentToolUseID, String content) {
        return new ProgressMessage(UUID.randomUUID().toString(), content, null, Instant.now(),
            toolUseID, parentToolUseID, null);
    }


    public static ToolResultBlock createToolResultStopMessage(String toolUseID) {
        return new ToolResultBlock(
                toolUseID,
                List.of(new TextBlock(MessageConstants.CANCEL_MESSAGE)),
                true
        );
    }


    public static List<UserMessage> createModelSwitchBreadcrumbs(
            String modelArg, String resolvedDisplay) {
        UserMessage caveat = createSyntheticUserCaveatMessage();
        // formatCommandInputTags equivalent (inlined to avoid cross-module dep).
        // Keep the whitespace byte-for-byte: it is part of the model-visible
        // transcript and therefore affects prompt-cache identity.
        String cmdInput = "<command-name>/model</command-name>\n"
                + "            <command-message>model</command-message>\n"
                + "            <command-args>" + modelArg + "</command-args>";
        UserMessage cmdMsg = createUserMessage(cmdInput);
        UserMessage stdout = createUserMessage(
                "<local-command-stdout>Set model to " + resolvedDisplay + "</local-command-stdout>");
        return List.of(caveat, cmdMsg, stdout);
    }

    // ── Remaining system message factories ───────────────────────────────────


    public static SystemMessage createPermissionRetryMessage(List<String> commands) {
        String content = "Allowed " + String.join(", ", commands);
        return new SystemMessage(UUID.randomUUID().toString(), "permission_retry", "info", content);
    }


    public static SystemMessage createBridgeStatusMessage(String url) {
        return new SystemMessage(UUID.randomUUID().toString(), "bridge_status", "info",
                "/remote-control is active. Code in CLI or at " + url);
    }


    public static SystemMessage createScheduledTaskFireMessage(String content) {
        return new SystemMessage(UUID.randomUUID().toString(), "scheduled_task_fire", "info", content);
    }


    public static SystemMessage createStopHookSummaryMessage(
            int hookCount, boolean preventedContinuation, String level) {
        String content = String.format("Stop hook summary: %d hook(s)%s",
                hookCount, preventedContinuation ? " (continuation prevented)" : "");
        return new SystemMessage(UUID.randomUUID().toString(), "stop_hook_summary", level, content);
    }


    public static SystemMessage createTurnDurationMessage(long durationMs) {
        return createTurnDurationMessage(durationMs, null);
    }

    /** Exact external-build transcript shape without an active turn token budget. */
    public static SystemMessage createTurnDurationMessage(long durationMs, Integer messageCount) {
        return createTurnDurationMessage(durationMs, messageCount, null, null);
    }

    /**
     * Turn-duration row that also records what was still running when the turn ended.
     */
    public static SystemMessage createTurnDurationMessage(
            long durationMs, Integer messageCount,
            Integer pendingBackgroundAgentCount, Integer pendingWorkflowCount) {
        return SystemMessage.turnDuration(UUID.randomUUID().toString(), Instant.now(),
            durationMs, messageCount, pendingBackgroundAgentCount, pendingWorkflowCount);
    }

    public static SystemMessage createTurnDurationMessage(
            long durationMs, Integer messageCount,
            Integer pendingBackgroundAgentCount, Integer pendingWorkflowCount,
            Long budgetTokens, Long budgetLimit, Integer budgetNudges,
            Integer briefHiddenCount) {
        return SystemMessage.turnDuration(UUID.randomUUID().toString(), Instant.now(),
            durationMs, messageCount, pendingBackgroundAgentCount, pendingWorkflowCount,
            budgetTokens, budgetLimit, budgetNudges, briefHiddenCount);
    }


    public static SystemMessage createAwaySummaryMessage(String content) {
        return new SystemMessage(UUID.randomUUID().toString(), "away_summary", "info", content);
    }


    public static SystemMessage createMemorySavedMessage(List<String> writtenPaths) {
        return createMemorySavedMessage(writtenPaths, "Saved");
    }

    public static SystemMessage createMemorySavedMessage(List<String> writtenPaths, String verb) {
        String content = verb + ": " + String.join(", ", writtenPaths);
        return new SystemMessage(UUID.randomUUID().toString(), "memory_saved", "info", content);
    }


    public static SystemMessage createAgentsKilledMessage() {
        return new SystemMessage(UUID.randomUUID().toString(), "agents_killed", "info", "");
    }


    public static SystemMessage createApiMetricsMessage(int ttftMs, double otps) {
        String content = String.format("API metrics: TTFT=%dms, OTPS=%.1f", ttftMs, otps);
        return new SystemMessage(UUID.randomUUID().toString(), "api_metrics", "info", content);
    }
}
