package com.claudecode.runtime.interaction;

/** Human interaction capabilities owned by the application runtime. */
public enum InteractionKind {
    PERMISSION,
    USER_QUESTION,
    SUDO_PASSWORD
}
